# Project-local Codex configuration for the architectural report skill.
#
# Skills are auto-discovered from `.agents/skills`.
# Custom subagents are auto-discovered from `.codex/agents/*.toml`.

[agents]
max_threads = 6
max_depth = 1
